// Test IDs for the home / landing feature.
// Keys are camelCase, values are kebab-case `<feature>-<element>`.

export const HOME = {
	// Removed emergent-branded test id to avoid visible third-party branding in production.
	mainLink: 'home-main-link',
};
